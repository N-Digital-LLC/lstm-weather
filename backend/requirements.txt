# Python 3.11
#
# GPU (default here): the CUDA 12.8 (cu128) wheel below supports modern NVIDIA
# GPUs including RTX 50-series / Blackwell (sm_120), which the old cu121 / 2.3.1
# wheels do NOT. The --extra-index-url makes pip pull the +cu128 build of torch
# while every other dependency still comes from PyPI.
#
# CPU-only fallback (no NVIDIA GPU): drop the --extra-index-url line and use a
# plain pin instead, e.g.:
#   torch==2.7.1
#
# Verify after install:
#   python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
#   -> expect e.g. 2.7.1+cu128 True

--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.1+cu128
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
scikit-learn==1.5.0
requests==2.32.3
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
matplotlib==3.9.0
