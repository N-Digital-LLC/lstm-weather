# Python 3.11
# Install torch with CUDA separately if you want GPU, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# The pinned torch below is the CPU/default wheel; override per your CUDA version.

torch==2.3.1
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
scikit-learn==1.5.0
requests==2.32.3
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
matplotlib==3.9.0
